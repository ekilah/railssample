<li>
  <%= gravatar_image_tag_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to "delete", user, method: :delete, data: { confirm: "Are you sure you wish to delete #{user.name} from the app?" } %>
  <% end %>
</li>